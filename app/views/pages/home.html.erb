<div class="flex-center mt-4" data-controller="counter">
  <div class="counter">
    <span data-counter-target="count"><%= @restaurant_count %></span>
  </div>

  <button class='btn btn-primary' data-action="click->counter#refresh">refresh counter</button>
</div>